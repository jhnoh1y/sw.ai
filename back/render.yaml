services:
  - type: web
    name: comadvisor-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: OPENAI_API_KEY
        sync: false  # Render 대시보드에서 직접 입력하도록 유도